import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roscolive.driverid',
  appName: 'RL Driver ID',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
